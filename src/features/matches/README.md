# features/matches

Domain module for **matches**. Group matches-specific components, hooks, and
server actions here as they're built out beyond the initial screens
already implemented in `src/components/matches` and `src/app`.

Not implemented yet — structural placeholder per the MITAN architecture spec.
