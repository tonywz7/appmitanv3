# BLOCKED: source code missing

`tentang_mitan_redesigned_about_page` was exported from Stitch with only `screen.png`.
No `code.html` was present in the original ZIP, so no markup could be normalized or moved here.

Only the preview image was recovered: `public/screens/about.png`.

**Action required:** re-export this screen from Google Stitch to obtain `code.html`.
