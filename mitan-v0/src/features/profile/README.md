# features/profile

Domain module for **profile**. Group profile-specific components, hooks, and
server actions here as they're built out beyond the initial screens
already implemented in `src/components/profile` and `src/app`.

Not implemented yet — structural placeholder per the MITAN architecture spec.
