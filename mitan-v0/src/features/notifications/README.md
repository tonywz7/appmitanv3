# features/notifications

Domain module for **notifications**. Group notifications-specific components, hooks, and
server actions here as they're built out beyond the initial screens
already implemented in `src/components/notifications` and `src/app`.

Not implemented yet — structural placeholder per the MITAN architecture spec.
