# features/onboarding

Domain module for **onboarding**. Group onboarding-specific components, hooks, and
server actions here as they're built out beyond the initial screens
already implemented in `src/components/onboarding` and `src/app`.

Not implemented yet — structural placeholder per the MITAN architecture spec.
